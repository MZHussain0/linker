type Props = {};

const UsernameFormResult = (props: Props) => {
  return (
    <div className="text-destructive font-semibold animate-pulse">
      Username already taken.
    </div>
  );
};

export default UsernameFormResult;
